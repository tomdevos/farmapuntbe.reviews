# Credentials for the Phil interaction portal (phil.apb.be).
# Copy this file to sources/.env and fill in the real values. The .env file is
# git-ignored — never commit it.

PHIL_USER=your-username@example.com
PHIL_PASS=your-password-here
